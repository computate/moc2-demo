apiVersion: v1
kind: ConfigMap
metadata:
  name: app-setup-playbook
  namespace: computate-lldap
data:
  app-setup.yaml: |
    ---
    - name: app-setup
      hosts: localhost
      connection: local
      gather_facts: false
      roles:
        - app-setup
