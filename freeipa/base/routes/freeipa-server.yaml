kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: freeipa-server
spec:
  host: freeipa.computate.org
  to:
    kind: Service
    name: freeipa
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

