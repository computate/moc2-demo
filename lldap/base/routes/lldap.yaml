kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: lldap
spec:
  host: freeipa.computate.org
  to:
    kind: Service
    name: lldap
    weight: 100
  port:
    targetPort: '3890'
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

